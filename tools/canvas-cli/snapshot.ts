/** SVG snapshot of one scope. */

import type { ArchitectureDocument } from '../../src/domain/model';

export function renderScopeSvg(_doc: ArchitectureDocument, _scopeId: string): string {
  throw new Error('snapshot: not yet implemented');
}
