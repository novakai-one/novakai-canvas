Read `AGENTS.md` before doing any work and follow it.

For browser verification, use only `node ~/.agents/browse/browse.mjs` with the
working directory set to `~/.agents/browse`. Never use or install repo-local
Playwright. Read `~/.agents/browse/README.md` before first use and run
`node browse.mjs close` when finished.
